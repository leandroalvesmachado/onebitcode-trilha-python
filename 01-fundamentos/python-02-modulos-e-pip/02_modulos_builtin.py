"""Módulos Builtin."""

# Em programação, quando falamos em módulos builtin (ou módulos embutidos), estamos nos referindo aos módulos 
# que já vêm incluídos na linguagem, sem precisar instalar nada adicional.

# Em outras palavras: são bibliotecas padrão que já fazem parte do núcleo da linguagem 
# e ficam disponíveis para uso imediato.

# Ele mostra uma lista de todos os módulos disponíveis no seu ambiente Python, incluindo:
# Módulos builtin (embutidos, que já vêm com o Python).
# Módulos instalados por você (via pip install).
help('modules')


# Exemplo de saída
# Please wait a moment while I gather a list of all available modules...

# __future__    _thread      heapq         select
# _abc          abc          hmac          selectors
# aifc          argparse     html          shelve
# array         asyncio      http          shlex
# base64        binascii     imaplib       shutil
# calendar      builtins     io            signal
# cmath         cProfile     json          socket
# ...
